[CONFIG]
# Configuration file for example_download_and_rasterize folder

# Name of the project
project: EMSR_692

# Path to the workspace containing the project
workspace: /data/scratch/globc/bonassies/workspace/swot_for_flood/examples

# Path to the data folder
data_path: /data/scratch/globc/bonassies/data

# Path to the AOI file
aoi: %(workspace)s/%(project)s/aux_data/EMSR692_aois_V2.gpkg

floodmask_path: %(workspace)s/%(project)s/aux_data/FloodedArea_32634_v2.gpkg
controlmask_path: %(workspace)s/%(project)s/aux_data/ControlArea_32634.gpkg
esa_worldcover_path: %(workspace)s/%(project)s/aux_data/ESA_WC_cut_V2_32634_nrow5720_ncol5917.tif

#  EPSG code of the CRS of the project
CRS= 32634

# CRS of the AOI file
aoi_crs= 4326

# First and last time of the data to download
first_time= 2023-01-01
last_time= 2025-06-01

# nodes to use for the download (optional)
nodes = 8

# Type of SWOT data to download (optional) can be PIXC, LakeSP, Nodes
download_type: PIXC

# Passes of interest to download (optional)
# passes = [402, 417]
passes = [402]

# Tile names to rasterize (optional)
# tile_names_selection: [[417_224L], [402_085L]]
tile_names_selection: [[402_085L, 402_086L]]

# List dry and flood dates
list_dry_dates: [2024-04-10, 2024-05-01, 2024-07-03, 2024-07-24]
list_flood_dates: [2023-09-15]

# variables to rasterize (optional)
# default: variables: [sig0, coherent_power, incidence, gamma_tot, gamma_SNR, gamma_est, power_plus_y, power_minus_y, interf_real, interf_imag, height, classification, bright_land_flag]

# Pixel resolution of the rasterized data (optional)
# pixel_resolution = 10

# GDAL environment variables for rasterization (optional)
GDAL_NUM_THREADS = 10
GDAL_CACHEMAX = 160000

# Boolean to activate automatic download, rasterize and save when loading the project python file (optional)
# It can be deactivate if everything has been already downloaded and rasterized
do_download = False
do_make_gpkg = False
do_make_tiff = False

