# Description: This file contains the variables used for plotting the results of the SWOT for Flood project.

# Define the labels for the plots
LABEL_SIG0 = '$10*log_{10}(\sigma_{0}) (dB)$'
LABEL_PCOH = "$10*log_{10}(P_{coherent}) (dB)$"
LABEL_HEIGHT = 'Height (m)'
LABEL_GAMMA_SNR = '$\gamma_{SNR}$'
LABEL_GAMMA_TOT = '$\gamma_{total}$'
LABEL_GAMMA_EST = '$\gamma_{estimate}$'